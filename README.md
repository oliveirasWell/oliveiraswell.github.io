# oliveiraswell.github.io

Personal page — React 19 + Vite, deployed to GitHub Pages by CI on every push to `master`.

```sh
yarn install
yarn dev      # dev server
yarn test     # vitest
yarn lint     # eslint
yarn build    # production build into dist/
```
